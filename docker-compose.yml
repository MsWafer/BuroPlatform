version: '3'
services:
  app:
    container_name: sysadminochka
    restart: always
    build: .
    ports:
      - '3672:3672'
    depends_on:
      - 'mongo'
  mongo:
    container_name: mongo_sysadminochka
    restart: always
    image: mongo
    command: mongod --port 27147
    ports: 
     - '27147:27147'
    volumes:
     - db-data:/data/db
     - mongo-config:/data/configdb

volumes:
  db-data: 
  mongo-config: